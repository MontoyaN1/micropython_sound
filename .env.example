# Archivo de configuración de variables de entorno - EJEMPLO
# Copiar este archivo a .env y reemplazar los valores con tus credenciales reales

# ========== INFLUXDB ==========
INFLUXDB_TOKEN=TU_TOKEN_INFLUXDB_AQUI
INFLUXDB_URL=https://tu-influxdb.easypanel.host
INFLUXDB_ORG=TU_ORGANIZACION
INFLUXDB_BUCKET=sensores

# ========== MQTT/EMQX ==========
MQTT_BROKER=213.199.37.1
MQTT_PORT=1883
MQTT_TOPIC=sensores/ruido
# MQTT_USERNAME=opcional
# MQTT_PASSWORD=opcional

# ========== BACKEND FASTAPI ==========
# REDIS_URL=redis://cache:6379  (se usa automáticamente en Docker Compose)

# ========== FRONTEND REACT ==========
VITE_API_URL=http://localhost:8000

# ========== APP DASH (legado) ==========
DASH_PORT=8050
DEBUG_MODE=false

# Instrucciones:
# 1. Copiar este archivo: cp .env.example .env
# 2. Editar .env con tus valores reales
# 3. Asegurarse de que .env esté en .gitignore
# 4. Nunca compartir el archivo .env real

# Notas:
# - El backend FastAPI usa INFLUXDB_TOKEN, INFLUXDB_URL, INFLUXDB_ORG, INFLUXDB_BUCKET
# - El backend se conecta a MQTT_BROKER:MQTT_PORT y se suscribe a MQTT_TOPIC
# - El frontend React consulta la API en VITE_API_URL
# - La app Dash legacy usa las mismas variables de InfluxDB